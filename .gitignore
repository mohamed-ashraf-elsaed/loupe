node_modules/
dist/
.playwright-mcp/
packages/server/data/
packages/extension/content.js
*.log
.DS_Store

# Claude Code setup — ALWAYS local, NEVER tracked (hard project rule).
# Covers the root and any nested package (patterns without a leading slash
# match at every depth), plus local memory files.
.claude/
**/.claude/
CLAUDE.md
CLAUDE.local.md
**/CLAUDE.md
**/CLAUDE.local.md
coverage/
loupe-extension.zip

# npm auth (CI writes this; never commit tokens)
.npmrc
