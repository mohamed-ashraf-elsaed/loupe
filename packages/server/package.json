{
  "name": "@loupekit/server",
  "version": "0.5.1",
  "description": "Loupe comment API — node:http, Postgres (PGlite locally / pg in prod), object storage for screenshots.",
  "type": "module",
  "scripts": {
    "start": "node index.ts",
    "seed": "node seed.ts"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "@loupekit/shared": "*",
    "pg": "^8.13.1"
  }
}
